#include <iostream>
using namespace std;
//Each Queries consist of 2 integers L and R 
//for Each query Geek has to find out number of Prime numbers in the range L,R 
int main() {
    int t;cin>>t;
    //But see the problem here as L,R <= 10^10 
    //So i can't make a sieve array of length that much 
    //But why it's written R-L <= 2*10^5 (length of Range does not exceed )
    
	return 0;
}